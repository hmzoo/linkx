http:
  routers:
    router0:
      rule: Host(`hmdev.mshome.net`)
      entrypoints : 
        - websecure
      service: app
      tls: {}
  services:
    app:
      loadbalancer:
        servers:
          - url: 'http://hmdev.mshome.net:5000'